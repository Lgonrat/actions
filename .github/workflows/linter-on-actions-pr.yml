name: Linter IA on Actions PR

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  run-linter:
    uses: Lgonrat/workflow-reusable/.github/workflows/linterIA_sinCopilot.yaml@main
